---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app connect-server
  namespace: openmu-prod
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    defaultPodOptions:
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: connect-server
        dapr.io/app-port: "8080"
    controllers:
      main:
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
          dapr.io/enabled: "true"
          dapr.io/app-id: login-server
          dapr.io/app-port: "8080"
        containers:
          main:
            image:
              repository: ktoanlba/openmu-connect
              tag: 8.2.2
            env:
              APPID: connect-server
              ASPNETCORE_URLS: http://+:8080
              ASPNETCORE_ENVIRONMENT: Production
            # resources:
            #   requests:
            #     cpu: 200m
            #     memory: 300Mi
            #   limits:
            #     cpu: 2000m
            #     memory: 3000Mi
    service:
      main:
        controller: main
        ports:
          http:
            port: 8080
          connect:
            port: 44405
          # Prometheus
          metrics:
            port: 9464

    ingress:
      main:
        enabled: false
